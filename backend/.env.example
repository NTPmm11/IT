# คัดลอกไฟล์นี้เป็น .env แล้วแก้ค่าให้ตรงเครื่องตัวเอง: cp .env.example .env
PORT=4000

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=it_change_request

# กุญแจเซ็น JWT — ตั้งเป็นข้อความสุ่มยาวๆ ห้ามใช้ค่าตัวอย่างนี้จริง
JWT_SECRET=change-me-to-a-long-random-string
